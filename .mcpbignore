# The bundle only needs the self-contained dist/index.mjs (esbuild already
# inlined every dependency) + the manifest. Everything below is source,
# tooling, or docs that don't belong in the shipped artifact.

.git/
.github/
.claude/
node_modules/
src/
scripts/
build.mjs
tsconfig.json
package.json
package-lock.json
.gitignore
.mcpbignore
glama.json
CHANGELOG.md
CONTRIBUTING.md
*.mcpb
**/__pycache__/
**/*.pyc
